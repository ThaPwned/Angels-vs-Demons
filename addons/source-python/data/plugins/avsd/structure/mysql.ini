players = "CREATE TABLE IF NOT EXISTS players (id INTEGER PRIMARY KEY AUTO_INCREMENT, uniqueid VARCHAR(64) UNIQUE, name VARCHAR(64), xp INTEGER DEFAULT 0, queue_xp INTEGER DEFAULT 0, level INTEGER DEFAULT 1, queue_level INTEGER DEFAULT 0, credit INTEGER DEFAULT 0, queue_credit INTEGER DEFAULT 0, prestige INTEGER DEFAULT 0, heirloom INTEGER DEFAULT 0)"
skills = "CREATE TABLE IF NOT EXISTS skills (name VARCHAR(64), id INTEGER, level INTEGER DEFAULT 0, queue_level INTEGER DEFAULT 0, heirloom INTEGER DEFAULT 0, PRIMARY KEY(id, name))"
player get = "SELECT id, name, xp, queue_xp, level, queue_level, credit, queue_credit, prestige, heirloom, (SELECT SUM(skills.heirloom) FROM skills WHERE skills.id = players.id) AS bonus FROM players WHERE uniqueid = %s"
player insert = "INSERT INTO players (id, uniqueid, name, credit) VALUES (NULL, %s, %s, %s)"
player update = "UPDATE players SET name = %s, xp = %s, queue_xp = %s, level = %s, queue_level = %s, credit = %s, queue_credit = %s, prestige = %s, heirloom = %s WHERE id = %s"
player reset = "UPDATE players SET xp = 0, level = 1, credit = %s, prestige = %s, heirloom = %s WHERE id = %s"
player skills = "SELECT name, level, queue_level, heirloom FROM skills WHERE id = %s"
rank update = "SELECT uniqueid, name, level FROM players ORDER BY level DESC"
skill insert = "INSERT INTO skills (name, id) VALUES (%s, %s)"
skill update = "UPDATE skills SET level = CASE %s END, queue_level = CASE %s END, heirloom = CASE %s END WHERE id = %%s"
skill join = "WHEN name = '%s' THEN %s"
create database = "CREATE DATABASE IF NOT EXISTS %s"
use database = "USE %s"
